// Real brand glyphs (react-icons). react-icons components accept `size` and
// `className` and inherit `currentColor`, so they drop into the same slots as
// lucide icons throughout the app.
export {
  FaWhatsapp as WhatsAppIcon,
  FaFacebookF as FacebookIcon,
  FaInstagram as InstagramIcon,
} from "react-icons/fa6";
